import React from 'react'

export default function WayOne () {
	return <div>
		<h2>第一种引进组件的方式</h2>
		<p>用function创建的组件，只有props，没有自己的私有数据和生命周期;运行效率高；使用率低；
			 两种组件创建的组件，props都是只读的；这种组件也叫：无状态组件：</p>
	</div>
}